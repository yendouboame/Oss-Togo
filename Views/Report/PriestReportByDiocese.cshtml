@model IEnumerable<IGrouping<Diocese, Priest>>

@{
    ViewData["Title"] = "Etat des prêtres par diocèse";
    Layout = "_ReportLayout";
}

<h2 class="text-center mb-4 border-bottom"><strong>@ViewData["Title"]</strong></h2>

<table class="table table-bordered text-center">
    @if (Model.Count() > 0)
    {
        @foreach (var group in Model)
        {
            int i = 1;
            <thead class="thead-dark">
                <tr>
                    <th colspan="5">@group.Key.Name</th>
                </tr>
            </thead>
            <thead class="thead-light">
                <tr>
                    <th>#</th>
                    <th class="text-left">@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().FullName)</th>
                    <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().DateOfBirth)</th>
                    <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().OrdinationDate)</th>
                    <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().Diocese)</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var priest in group)
                {
                    <tr>
                        <td>@(i++)</td>
                        <td class="text-left">@priest.FullName</td>
                        <td>@priest.DateOfBirth.ToShortDateString()</td>
                        <td>@priest.OrdinationDate.ToShortDateString()</td>
                        <td>@priest.Diocese?.Name</td>
                    </tr>
                }
            </tbody>
        }
    }
    else
    {
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().FullName)</th>
                <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().DateOfBirth)</th>
                <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().OrdinationDate)</th>
                <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().Diocese)</th>
            </tr>
        </thead>
        <tbody>
            <tr role="group" class="bg-light text-center">
                <td colspan="4">Aucune entrée correspondante trouvée</td>
            </tr>
        </tbody>
    }
</table>


@*@model IEnumerable<IGrouping<Diocese, Priest>>

@{
    ViewData["Title"] = "Etat des prêtres par diocèse";
    Layout = "_ReportLayout";
}

<div class="row">
    <div class="col-md-12 my-4">
        <div class="card shadow">
            <div class="card-body">
                <h5 class="card-title"><strong>@ViewData["Title"]</strong></h5>
                <hr />

                <div id="accordion">
                    @foreach (var group in Model)
                    {
                        <div class="card">
                            <div class="card-header" id="heading@(group.Key.Id)">
                                <h5 class="mb-0">
                                    <button class="btn btn-link" data-toggle="collapse" data-target="#collapse@(group.Key.Id)" aria-expanded="true" aria-controls="collapse@(group.Key.Id)">
                                        @group.Key.Name
                                    </button>
                                </h5>
                            </div>

                            <div id="collapse@(group.Key.Id)" class="collapse" aria-labelledby="heading@(group.Key.Id)" data-parent="#accordion">
                                <div class="card-body">
                                    <table class="data-table table table-bordered table-hover text-center">
                                        <thead>
                                            <tr>
                                                <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().FullName)</th>
                                                <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().DateOfBirth)</th>
                                                <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().OrdinationDate)</th>
                                                <th>@Html.DisplayNameFor(model => model.Key.Priests.FirstOrDefault().Diocese)</th>
                                            </tr>
                                        </thead>
                                        <tbody>
                                            @foreach (var priest in group)
                                            {
                                                <tr>
                                                    <td>@priest.FullName</td>
                                                    <td>@priest.DateOfBirth.ToShortDateString()</td>
                                                    <td>@priest.OrdinationDate.ToShortDateString()</td>
                                                    <td>@priest.Diocese?.Name</td>
                                                </tr>
                                            }
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>*@